import styled from 'styled-components';

const Card = styled.div`
  width: 125px;
  height: 200px;
  border-radius: 1em;
  background-color: #ccc;
`;

const Styled = {
  Card,
};

export default Styled;
